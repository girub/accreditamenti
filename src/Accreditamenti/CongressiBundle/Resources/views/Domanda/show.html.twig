<a href="{{ path('risposta_new', { 'domanda_id': entity.id }) }}">
    <span class="icon-plus-sign icon-white"></span> Crea risposta
</a>

<h1>Domanda</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Descrizione</th>
            <td>{{ entity.descrizione }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('domanda') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('domanda_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('domanda_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
