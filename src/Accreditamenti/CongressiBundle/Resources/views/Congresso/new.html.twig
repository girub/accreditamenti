{% extends '::base.html.twig' %}

{% block body %}

    <div class="navbar">
        <div class="navbar-inner">
            <div class="container">
                <ul class="nav">
                    <li>
                        <a href="{{ path('congresso') }}">
                            <span class="icon-backward icon-white"></span> Back to the list
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
        
    <h1>Congresso creation</h1>

    <form action="{{ path('congresso_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>
{% endblock %}