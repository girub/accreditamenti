{% extends '::backEnd/base.html.twig' %}

{% block body %}

    <h1>Risposta creation</h1>

    <form action="{{ path('risposta_ecm_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

    <ul class="record_actions">
        <li>
            <a href="{{ path('risposta', {'domanda_id': domanda_id}) }}">
                Back to the list
            </a>
        </li>
    </ul>

{% endblock %}