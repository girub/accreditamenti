{% extends '::frontEnd/base.html.twig' %}

{% block body %}
<h1>Benvenuto - compila Anagrafica</h1>

qui devo far compilare l'anagrafica all'iscritto che è appena entrato inserendo il suo codice fiscale


<h1>Anagrafica creation</h1>

<form action="{{ path('anagrafica_create', {'accreditamento_id': accreditamento_id}) }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('anagrafica') }}">
            Back to the list
        </a>
    </li>
</ul>



{% endblock %}